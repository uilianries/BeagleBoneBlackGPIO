# Become Ubuntu 14.04 Trusty 
sudo: required
dist: trusty

language: 
  - cpp

compiler:
  - gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq -y cmake libboost-all-dev cppcheck clang-3.6

script:
  - script/linter.sh
  - script/static-analysis.sh
  - mkdir build
  - cd build
  - cmake .. && make && test/test_gpio

branches:
  only:
    - develop
