# Become Ubuntu 14.04 Trusty 
sudo: required
dist: trusty

language: cpp

compiler:
  - gcc
  - clang
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-5


os:
  - linux

before_install:
  - sudo apt-get install -y -qq cmake libboost1.55-all-dev cppcheck clang-3.6 clang-format-3.6 valgrind doxygen curl
  - git clone https://github.com/uilianries/3rdParty.git && cd 3rdParty && sudo dpkg -i libpoco_1.7-amd64.deb && cd ..

install:
  if [ "$CXX" = "g++" ]; then export CXX="g++-5" CC="gcc-5"; fi

script:
  - script/doxygen.sh
  - script/build.sh --install
  - script/valgrind.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)

branches:
  only:
    - develop
    - master
